// Postgres schema creation script has been retired. The project was reverted
// to using SQLite for simplicity when deploying to Render/Vercel in the current plan.
// If you need to recreate the script, restore it from git history.
